* Do not edit generated files; they are always fully regenerated by `make`.
* Always check that the documentation in `doc.go` and `musta/doc.go` matches
  the actual signature and behaviour if you changed anything.
* Always run `make` in the end until it reports no issues if you changed anything.
